name: Cypress Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: apps/frontend
        run: npm ci

      - name: Run Cypress tests
        working-directory: apps/frontend
        run: npx cypress run --record --key 7db637dc-085d-42e6-8317-8d70a957ff69